# Set shut down, restart and locking features

set $mode_system (l)ock, (e)xit, switch_(u)ser, (s)uspend, (h)ibernate, (r)eboot, (Shift+s)hutdown
bindsym $super+0 mode "$mode_system"
mode "$mode_system" {
    bindsym l exec --no-startup-id session lock, mode "default"
    bindsym s exec --no-startup-id session suspend, mode "default"
    bindsym u exec --no-startup-id session switch_user, mode "default"
    bindsym e exec --no-startup-id session logout, mode "default"
    bindsym h exec --no-startup-id session hibernate, mode "default"
    bindsym r exec --no-startup-id session reboot, mode "default"
    bindsym Shift+s exec --no-startup-id session shutdown, mode "default"

    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
