# vim: filetype=sh nospell

# if `wofi` package is installed

# Start your launcher
bindsym $alt+2 exec swaymsg exec $(wofi --show run)

# start bookmark menu
bindsym $alt+3 exec $HOME/projects/scripts/bin/bookmarks-gui
