#!/bin/bash

echo \
"Control key symbol ✲  represents Ctrl-a

    ✲  ?           │ show key binding
    ✲  r           │ reload ~/.tmux.conf
    ✲  x           │ close pane
    ✲  c           │ new window
    ✲  ,           │ rename window
    ✲  n|p         │ next/previous window
    ✲  w           │ list windows
    ✲  d           │ detach from session (run tmux attach)
    ✲  a           │ send prefix key to program in pane 

          copy & paste (Vi style)

    ✲  [           │ copy mode
    q, Esc         │ exit copy mode
    ✲  /           │ copy mode, start search
    n              │ next result
    ⇧ n            │ previous result 
    h,j,k,l        │ navigate to selection
    v,Space        │ start selection
    y,Enter        │ copy selection
    ✲  ]           │ paste selection

             plugin manager

    ✲  I           │ install plugins in ~/.tmux/plugins/
    ✲  U           │ update plugins
    ✲  ⎇   u       │ uninstall plugins

              yank plugin

    y              │ copy mode selection to clipboard
    ✲  y           │ copy text from CLI to clipboard
    ✲  Y           │ copy working directory to the clipboard

           pain control plugin

    ✲  h|j|k|l     │ select pane left|below|above|right
    ✲  ⇧  h|j|k|l  │ resize pane left|down|up|right 
    ✲  |           │ split current pane horizontally
    ✲  -           │ split current pane vertically
    ✲  \           │ split current pane full width horizontally
    ✲  _           │ split current pane full width vertically

             copycat plugin

    ✲  /           │ regex search (strings work too)
    n              │ jumps to the next match
    N              │ jumps to the previous match
    ⏎              │ copy a highlighted match
                     
Simultaneous keyboar│d input on all panes:

    Ctrl-a :setw synchronize-panes [on|off]
"
