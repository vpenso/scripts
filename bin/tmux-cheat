#!/bin/bash

echo \
"Hold Shift to select text with mouse.

    Ctrl-a ?       │ show key binding
    Ctrl-a r       │ reload ~/.tmux.conf
    Ctrl-a -       │ new horizontal pane
    Ctrl-a |       │ new vertical pane  
    Alt ← ↑ → ↓    │ move between panes
    Ctrl-a ← ↑ → ↓ │ resize panes
    Ctrl-a x       │ close pane
    Ctrl-a c       │ new window
    Ctrl-a ,       │ rename window
    Ctrl-a n/p     │ next/previous window
    Ctrl-a w       │ list windows
    Ctrl-a d       │ detach from session (run tmux attach)
    Ctrl-a a       │ send prefix key to program in pane 

Copy & paste (Vi style)

    Ctrl-a [       │ copy mode
    q, Esc         │ exit copy mode
    Ctrl-a /       │ copy mode, start search
    n              │ next result
    Shift-n        │ previous result 
    h,j,k,l        │ navigate to selection
    v,Space        │ start selection
    y,Enter        │ copy selection
    Ctrl-a ]       │ paste selection

             plugin manager

    Ctrl-a I       │ install plugins in ~/.tmux/plugins/
    Ctrl-a U       │ update plugins
    Ctrl-a Alt u   │ uninstall plugins

              yank plugin

    y              │ copy mode selection to clipboard
    Ctrl-a y       │ copy text from CLI to clipboard
    Ctrl-a Y       │ copy working directory to the clipboard

Simultaneous keyboard input on all panes:

    Ctrl-a :setw synchronize-panes [on|off]
"
